# Smart Autoscaler - Production Dependencies
# Optimized for lightweight Docker images

# Core Kubernetes & Monitoring
kubernetes==29.0.0
prometheus-api-client==0.5.5
prometheus-client==0.19.0

# Web Dashboard
flask==3.0.0
flask-cors==4.0.0

# Data Processing (lightweight)
numpy==1.26.4
pyyaml==6.0.1

# HTTP & Networking
requests==2.31.0
urllib3==2.2.0

# System Monitoring
psutil==5.9.8

# Logging
python-json-logger==2.0.7

# Machine Learning (optional - only if predictive scaling enabled)
# These are heavy dependencies - consider making them optional
scikit-learn==1.3.2
scipy==1.11.4
statsmodels==0.14.1

# GenAI Integration (optional - only install if ENABLE_GENAI=true)
# Note: These should be installed separately to keep base image lightweight
# openai>=1.0.0
# google-generativeai>=0.5.0
# anthropic>=0.18.0
